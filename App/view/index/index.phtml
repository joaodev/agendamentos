<?php
$canViewExpenses = $this->acl($_SESSION['ROLE'], $this->resourceCodes('view'), $this->moduleCodes('expenses'));
$canViewSchedules = $this->acl($_SESSION['ROLE'], $this->resourceCodes('view'), $this->moduleCodes('schedules'));
?>
<section class="content mt-5 pt-4" style="margin-left: 9px; margin-right: 9px;">
    <div class="container-fluid mt-2">
        <div class="row">
            <?php if ($this->view->isAdmin || $canViewExpenses): ?>
                <div class="col-6 col-md-4">
                    <div class="small-box bg-purple" onclick="openModule('contas')" style="cursor: pointer;">
                        <div class="inner text-white">
                            <p>Contas <br>Pendentes</p>
                            <h3>
                                <?php echo $this->view->total_pending_expenses; ?>
                            </h3>
                        </div>
                        <div class="icon">
                            <i class="fas fa-file-invoice-dollar text-white"></i>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
            <?php if ($this->view->isAdmin || $canViewSchedules): ?>
                <div class="col-6 col-md-4">
                    <div class="small-box bg-orange" onclick="openModule('agendamentos')" style="cursor: pointer;">
                        <div class="inner text-white">
                            <p>Agendamentos <br>Pendentes</p>
                            <h3>
                                <?php echo $this->view->total_pending_schedules; ?>
                            </h3>
                        </div>
                        <div class="icon">
                            <i class="fas fa-calendar text-white"></i>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
            <?php if ($this->view->isAdmin || $canViewSchedules): ?>
                <div class="col-6 col-md-4">
                    <div class="small-box bg-green" onclick="openModule('agendamentos')" style="cursor: pointer;">
                        <div class="inner text-white">
                            <p>Novos <br>Clientes</p>
                            <h3>
                                <?php echo $this->view->total_pending_schedules; ?>
                            </h3>
                        </div>
                        <div class="icon">
                            <i class="fas fa-user-plus text-white"></i>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
        </div>
    </div>
</section>